# =============================
# **Mission 5 - Who lived here?**
# =============================
# **Mission 6 - Forge**
# **Mission 8 - Silo**
# **Mission 9 - Whats on sale?**
# **Mission 10 - Tip the scales**
# Import necessary modules from the Hub SDK
from hub import light_matrix, port
import runloop
import motor_pair
import motor
from hub import motion_sensor

def forward(number):
    return number

def backward(number):
    return -number

async def move_straight_for_degrees(left_motor:int, right_motor:int, degrees:int, speed:int) -> None:
    # Pair the motors if needed
    #motor_pair.pair(motor_pair.PAIR_1, left_motor, right_motor)

    # Reset the relative position of both motors
    motor.reset_relative_position(left_motor, 0)
    motor.reset_relative_position(right_motor, 0)

    # Reset the yaw angle to zero and wait for stabilization
    motion_sensor.reset_yaw(0)
    #await runloop.until(motion_sensor.stable)

    # Set the target angle (0 degrees for straight movement)
    target_angle = 0

    # Define the proportional gain for correction
    Kp = 0.08 # Adjust this value based on your robot's behavior

    # Loop to maintain straight movement
    while (abs(motor.relative_position(left_motor))) < abs(degrees):
        # Get the current yaw angle
        current_angle = motion_sensor.tilt_angles()[0]
        # Calculate the error
        error = target_angle - current_angle # Corrected to target - current
        # Calculate the correction
        correction = int(Kp * error)
        # Adjust the motor speeds to apply correction for straight movement
        left_speed = speed + int(correction)
        right_speed = speed - int(correction)
        # Move the robot with corrected motor speeds
        motor_pair.move_tank(motor_pair.PAIR_1, left_speed, right_speed)
        # Small delay for stability
        await runloop.sleep_ms(10)
    # Stop the motors after the loop
    motor_pair.stop(motor_pair.PAIR_1)

    # You run the normal move_for_degrees function.
    ## CODE STARTS HERE

async def main():
    # Pair motors for movement
    motor_pair.pair(motor_pair.PAIR_1, port.F, port.E)
    # Get initial angle after reset
    new_angle = motion_sensor.tilt_angles()[0]
    print('after reset', new_angle)  # after reset
    # First straight before first turn
    #await move_straight_for_degrees(port.F, port.E, 100, 600)
    await move_straight_for_degrees(port.F, port.E, forward(100), 600)

    # First turn to turn toward mission
    await motor_pair.move_tank_for_degrees(motor_pair.PAIR_1, 67, 0, 600)

    # Go forward towards mission
    await motor_pair.move_for_degrees(motor_pair.PAIR_1, 1350, 0, velocity=600)

    # Turn towards who lived here
    await motor_pair.move_tank_for_degrees(motor_pair.PAIR_1, 280, 0, 600)

    # Go back to push the thingy and raise the board
    #await motor_pair.move_for_degrees(motor_pair.PAIR_1, -170, 0, velocity=600)
    await motor_pair.move_for_degrees(motor_pair.PAIR_1, backward(170), 0, velocity=600)

    # Go forward to make sure we can do turn towards whats on sale
    await move_straight_for_degrees(port.F, port.E, 150, 600)
    # new_angle = motion_sensor.tilt_angles()[0]
    # print('go forward to turn to market', new_angle)

    # Turn to whats on sale
    await motor_pair.move_tank_for_degrees(motor_pair.PAIR_1, 500, 0, 600)

    # Arm goes down
    # new_angle = motion_sensor.tilt_angles()[0]
    # print('arm goes down', new_angle)

    # Do whats on sale using the attached bar
    await move_straight_for_degrees(port.F, port.E, 300, 200)
    # Print angle after pushing table
    new_angle = motion_sensor.tilt_angles()[0]
    print('pushing table', new_angle)
    await motor_pair.move_for_degrees(motor_pair.PAIR_1, backward(170), 0, velocity=600)
    # Print angle after going back
    new_angle = motion_sensor.tilt_angles()[0]
    print('go back', new_angle)

    # Turn towards the basket
    await motor_pair.move_tank_for_degrees(motor_pair.PAIR_1, 200, -100, 600)
    # Print angle after turning to basket
    new_angle = motion_sensor.tilt_angles()[0]
    print('turn to basket', new_angle)

    # Raise the arm
    await motor.run_for_degrees(port.D, 30, 500)
    # Move back from basket
    await motor_pair.move_for_degrees(motor_pair.PAIR_1, backward(250), 0, velocity=600)
    # Lower the arm
    await motor.run_for_degrees(port.D, -20, 500)
    # Move straight after basket
    await move_straight_for_degrees(port.F, port.E, 500, 200)

runloop.run(main())
